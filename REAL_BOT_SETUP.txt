Boostora / @BoostoraBot

1. Copy .env.example to .env
2. Paste your real bot token into BOT_TOKEN=
3. ADMIN_IDS is already set to 2097006037
4. SUPPORT_USERNAME is already set to @BoostoraBot
5. Run run.bat on Windows or python -m app.main

Important: the token you shared in chat is compromised. Rotate it in BotFather before any real launch.

6. REQUIRED_CHAT_ID is already set to -4998535978
7. If the required chat is private, paste its invite link into REQUIRED_CHAT_INVITE_LINK=
8. Add the bot to that chat; making it an admin is the safest way to keep membership checks stable.
